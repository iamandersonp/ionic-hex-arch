import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.demo.hexagonal',
  appName: 'TV Maza',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
